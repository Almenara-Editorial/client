import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  align-items: center;
  margin-left: auto;
  gap: 1.2rem;
  margin-top: var(--space-lg);

  > div {
    flex: 1;
  }
`
