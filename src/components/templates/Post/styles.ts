import { Wrapper } from '@/components/shared'
import styled from 'styled-components'

export const Container = styled(Wrapper).attrs({ as: 'article' })`
  width: 100%;
  max-width: 66.64rem;
  margin: 0 auto;
  padding-block: 4.8rem;
`
