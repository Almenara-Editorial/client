import { ArrowSolid } from '.'
import { Story, Meta } from '@storybook/react'

export default {
  title: 'icons/ArrowSolid',
  component: ArrowSolid
} as Meta

export const Basic: Story = (args) => <ArrowSolid {...args} />
